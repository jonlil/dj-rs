# Rust build artifacts
/target/
**/*.rs.bk

# Rekordbox data — personal library exports, not for version control
rekordbox.xml
rekordbox_db_structure/

# Dev/test sandbox music files
sandbox/

# Utility scripts not part of the app
create_sandbox.py

# Claude Code working directory
.claude/

# Compressed archives
/*.tar.gz
/*.zip
/*.tar.bz2
/*.tar.xz
/*.7z

# Logs
*.log

# Tizen app build output and signed packages
tizen-app/dist/
tizen-app/*.wgt
tizen-app/target/

# dj_jonas library export (large audio files + personal DB)
dj_jonas/

# Tizen signing certificates (stored in ~/tizen-studio-data, never in repo)
*.p12
*.pem
*.cer
profiles.xml

# Tizen local env (cert password, not for version control)
tizen-app/.env.tizen

# Generated test fixtures (ffmpeg sine tones)
tests/fixtures/
